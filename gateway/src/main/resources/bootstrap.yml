spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: false
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

eureka:
  client:
    serviceUrl:
      defaultZone=http://user:password@localhost:8765/eureka: